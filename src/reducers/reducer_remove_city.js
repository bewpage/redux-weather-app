import { REMOVE_CITY } from '../constants';


const removeById = (state = [], id) => {
    // const cityList = state.filter(city => city.id !== id);
    console.log('remove city', cityList)
    return cityList;
};

